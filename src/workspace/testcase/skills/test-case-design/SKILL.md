---
name: test-case-design
description: 核心测试设计Skill，提供完整的测试用例设计方法论。当需要为具体功能点设计测试用例时激活，提供等价类划分、边界值分析、决策表、状态转换、场景法、错误推测等六大经典设计技术，以及完整的用例模板和设计规范。
---

# 测试用例设计方法论 Skill

## 激活场景
- 需要为具体功能点生成测试用例
- 用户要求设计某模块或接口的测试用例
- 评估现有用例是否覆盖足够的设计维度
- 用户提供了测试用例样例，并要求后续生成结果按样例风格输出时，应同时遵循 `testcase-sample-style` Skill

---

## 六大测试设计技术

### 技术一：等价类划分（Equivalence Partitioning）
**原理**：将输入域划分为若干等价类，每类中任意取一个值进行测试。

```
应用步骤：
1. 识别所有输入字段
2. 对每个字段划分有效等价类 + 无效等价类
3. 从每个等价类中取一个代表性值

示例 - 用户名字段（长度4-20个字符）：
┌─────────────────┬──────────────────┬─────────────────────┐
│ 等价类           │ 类别             │ 代表值               │
├─────────────────┼──────────────────┼─────────────────────┤
│ 长度4-20字符     │ ✅ 有效等价类     │ "testuser"（8位）    │
│ 长度1-3字符      │ ❌ 无效等价类     │ "abc"（3位）         │
│ 长度21+字符      │ ❌ 无效等价类     │ "a"×21（21位）       │
│ 含特殊字符       │ ❌ 无效等价类     │ "user@name"          │
│ 纯数字           │ ⚠️ 待定等价类    │ "12345678"           │
└─────────────────┴──────────────────┴─────────────────────┘
```

### 技术二：边界值分析（Boundary Value Analysis）
**原理**：在等价类边界处取值，错误往往出现在边界。

```
边界值选取规则（对于范围 [min, max]）：
  min-1  |  min  |  min+1  |  中间值  |  max-1  |  max  |  max+1

示例 - 年龄范围（18-65岁）：
┌──────┬─────────────────────────────────────────┐
│ 值   │ 用例说明                                  │
├──────┼─────────────────────────────────────────┤
│  17  │ 下边界-1（期望：拒绝，提示"年龄不符合要求"） │
│  18  │ 下边界（期望：接受）                        │
│  19  │ 下边界+1（期望：接受）                      │
│  41  │ 中间值（期望：接受）                        │
│  64  │ 上边界-1（期望：接受）                      │
│  65  │ 上边界（期望：接受）                        │
│  66  │ 上边界+1（期望：拒绝）                      │
│   0  │ 零值特殊处理（期望：拒绝）                   │
│  -1  │ 负数特殊处理（期望：拒绝）                   │
└──────┴─────────────────────────────────────────┘

字符串边界：空字符串("") / 单字符 / 最大长度 / 最大长度+1 / 超长字符串
数字边界：0 / 负数 / 最大整数 / 小数精度边界
日期边界：年份起止 / 月份28/29/30/31日 / 闰年 / 跨年
```

### 技术三：决策表法（Decision Table）
**原理**：用于多条件组合的业务规则测试，穷举条件组合。

```
示例 - 优惠券使用规则：
条件：
  C1: 订单金额 ≥ 100元
  C2: 用户是会员
  C3: 优惠券未过期

结果：
  A1: 可使用优惠券
  A2: 不可使用优惠券
  A3: 显示满减提示

┌────┬────┬────┬────┬────┬────┬────┬────┬────┐
│    │ 1  │ 2  │ 3  │ 4  │ 5  │ 6  │ 7  │ 8  │
├────┼────┼────┼────┼────┼────┼────┼────┼────┤
│ C1 │ T  │ T  │ T  │ T  │ F  │ F  │ F  │ F  │
│ C2 │ T  │ T  │ F  │ F  │ T  │ T  │ F  │ F  │
│ C3 │ T  │ F  │ T  │ F  │ T  │ F  │ T  │ F  │
├────┼────┼────┼────┼────┼────┼────┼────┼────┤
│ A1 │ ✅ │ ❌ │ ❌ │ ❌ │ ❌ │ ❌ │ ❌ │ ❌ │
│ A2 │    │ ✅ │ ✅ │ ✅ │ ✅ │ ✅ │ ✅ │ ✅ │
└────┴────┴────┴────┴────┴────┴────┴────┴────┘
→ 每列生成一个测试用例（可合并规则相同的列）
```

### 技术四：状态转换法（State Transition）
**原理**：针对对象状态机进行测试，验证状态转换的正确性。

```
应用对象：订单、用户账号、审批流程、工单状态

状态图示例 - 订单状态：
[待付款] --支付成功--> [待发货]
[待付款] --取消订单--> [已取消]
[待付款] --支付超时--> [已关闭]
[待发货] --商家发货--> [待收货]
[待发货] --取消申请--> [退款中]
[待收货] --确认收货--> [已完成]
[待收货] --申请退货--> [退款中]
[退款中] --退款成功--> [已退款]

测试用例设计：
1. 正向流程：待付款→待发货→待收货→已完成
2. 取消流程：待付款→已取消（取消按钮是否消失）
3. 超时流程：待付款→已关闭（超时触发）
4. 退款流程：待收货→退款中→已退款
5. 非法转换：已完成→不可再次申请退款（异常验证）
```

### 技术五：场景法（Scenario Method）
**原理**：模拟真实用户操作路径，串联多步骤的端到端场景。

```
场景构建公式：
  基本流（主成功场景）+ 备选流（分支/异常场景）

示例 - 电商下单场景：

基本流：
  选商品→加入购物车→选地址→选优惠券→提交订单→支付→完成

备选流清单：
  AF1: 商品库存不足（选商品后提示售罄）
  AF2: 地址为空（提交时提示添加地址）
  AF3: 优惠券不满足条件（不可选中灰显）
  AF4: 支付失败（跳转支付失败页，提供重试）
  AF5: 支付超时（订单关闭，返回商品列表）
  AF6: 网络中断（支付中断，订单状态处理）
  AF7: 重复提交（防重复下单校验）

每条备选流 = 1个完整测试用例
```

### 技术六：错误推测法（Error Guessing）
**原理**：基于经验和直觉推测最可能出现错误的场景。

```
高价值错误推测点：

【数据层】
- 空值：null / "" / " "（空白字符串） / undefined
- 特殊字符：' " < > & ; # % @ \ / | * ? 
- 超长输入：超过数据库字段长度限制
- 类型混淆：数字字段传字符串，日期格式错误
- 编码问题：中文、emoji、特殊Unicode字符

【业务层】
- 重复操作：快速双击提交按钮
- 并发操作：同时扣减库存/积分
- 跨会话操作：A用户操作B用户的数据
- 状态不一致：前端显示与后端状态不同步

【系统层】
- 文件上传：超大文件 / 0字节文件 / 可执行文件
- 图片处理：非图片文件改扩展名上传
- 时区问题：跨时区的时间计算
- 精度问题：金额计算的小数精度丢失
```

---

## 标准测试用例模板

## 样例风格优先规则（新增/编辑/删除/查询）

当用户提供了测试用例样例时，生成的测试用例在以下三个字段上必须优先贴合样例风格：
- 前置条件
- 测试步骤
- 预期结果

当存在样例文件或用户明确要求“按样例标准写”时，应优先遵循 `testcase-sample-style` Skill 中定义的硬性规则、固定模板和执行充分性要求；本节作为设计阶段的摘要提醒。

### 一、前置条件写法
- 使用编号列表逐条描述。
- 优先写清：登录系统、权限、菜单入口、页面位置、基础测试数据。
- 不要只写“已进入页面”这种过度简化描述，应尽量像人工测试文档那样完整。

推荐风格示例：
```text
1.返回XX业务系统首页；
2.用户使用有效账号登录系统；
3.当前账号具备目标模块的新增/编辑/删除/查询权限；
4.系统中已存在用于编辑、删除或查询的测试数据；
```

### 二、测试步骤写法
- 使用编号列表，每一步是完整中文操作句。
- 风格尽量贴近样例：`点击`、`输入`、`选择`、`打开`、`确认`、`保存`、`查询`。
- 针对新增、编辑、删除、查询四类操作，优先沿用样例中的业务动作链路。
- **从本规则开始，测试步骤与预期结果必须按序一一对应：步骤有几条，预期结果就必须有几条。**
- **中间步骤也必须有对应结果，不能只给最后的业务结果。**

推荐动作骨架：
```text
新增：进入列表页 → 点击新增 → 输入/选择字段 → 点击保存
编辑：进入列表页 → 定位目标记录 → 点击编辑 → 修改字段 → 点击保存
删除：进入列表页 → 定位目标记录 → 点击删除 → 点击确认
查询：进入列表页 → 输入/选择查询条件 → 点击查询
```

若需求文档没有明确要求省略导航，则步骤建议保持从进入功能页面开始的连续操作，不要只写页面内部孤立动作。

### 三、预期结果写法
- 使用编号列表。
- 风格贴近人工验收文档，不用过度技术化术语堆砌。
- 重点围绕页面展示、提示信息、列表结果、字段值变化、数据是否生效来写。
- **每一个测试步骤都必须对应一条预期结果，条数必须与步骤条数完全一致。**
- **若最后一步存在多个检查点，必须合并进最后一步对应的那一条预期结果。**

推荐结果骨架：
```text
新增：新增成功，并在列表中显示新记录；
编辑：修改成功，再次查看时显示修改后的内容；
删除：删除成功，列表中不再显示该记录；
查询：返回符合条件的记录，不符合条件的记录不显示；
```

### 四、允许扩展的边界
- 可以在样例风格基础上补充更完整的字段校验、异常流和边界值。
- 但扩展后仍要保持样例那种“人工可直接执行”的中文文档风格。
- 不要改写成过于抽象的测试点清单，也不要只输出程序化 JSON 风格语句。

```markdown
### [TC-模块缩写-序号]：[用例标题，动词+对象+场景]

| 字段 | 内容 |
|------|------|
| **用例编号** | TC-[MODULE]-[NNN] |
| **用例标题** | [动词][操作对象][条件/场景] |
| **所属模块** | [模块名称] |
| **用例类型** | 功能测试 / 接口测试 / 安全测试 / 性能测试 |
| **设计方法** | 等价类 / 边界值 / 决策表 / 状态转换 / 场景法 / 错误推测 |
| **优先级** | P0 / P1 / P2 / P3 |
| **前置条件** | [执行前必须满足的具体条件，可验证] |

**测试步骤**：
1. [第一步操作，包含操作对象和具体操作]
2. [第二步操作]
3. [第N步操作]

**测试数据**：
```
字段名: 具体值（说明值的来源或意义）
```

**预期结果**：
1. [可验证的预期结果，避免"正确""成功"等模糊描述]
2. [如有多个验证点，逐条列出]
3. [包含UI显示、数据库状态、接口返回的全面验证]

**备注**：[关联需求编号 REQ-XXX / 设计文档链接 / 特殊注意事项]
```

---

## 设计质量红线

❌ **禁止出现**以下情况（如出现需立即修正）：
- 预期结果使用"正确显示"、"正常运行"、"成功"等无法客观验证的描述
- 一个用例包含多个不相关的验证点
- 步骤中使用"等待片刻"、"稍后"等无法量化的时间描述
- 前置条件不可重现（如"用户刚刚进行过某操作"）
- 缺少关键测试数据导致用例无法独立执行

